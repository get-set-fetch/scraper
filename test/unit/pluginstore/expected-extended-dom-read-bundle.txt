class Base {
}

class ExtendedDomRead extends Base {
  constructor() {
    super(...arguments);
    this.opts = {
      domRead: true,
    };
  }

  sum(a, b) {
    return a + b;
  }

  async asum(a, b) {
    return a + b;
  }
}
