class Base {
  sum(a, b) {
    return 0;
  }
}

var Base_1 = Base;

// eslint-disable-next-line @typescript-eslint/no-var-requires


class ExtendedDomRead extends Base_1 {
  opts = {
    domRead: true,
  }

  sum(a, b) {
    return a + b;
  }

  async asum(a, b) {
    return a + b;
  }
}

var ExtendedDomRead_1 = ExtendedDomRead;
